services:
  - type: web
    name: slbfe-api
    runtime: node
    buildCommand: cd server && npm install
    startCommand: cd server && node seed.js && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: slbfe-production-secret-key-2024-change-this
